// Given a vector of contractions and a dictionary 
// of words, output a vector of all words that can
// be formed using just those strings.

#include <iostream>
#include <vector>

using svec_t = std::vector<std::string>;
using s_t = std::string;

svec_t generateContractions(
        const svec_t& dictionary
    ,   svec_t& contractions
    ) {
    // do stuff;
}


